const WebLogo = ({ custom }) => {
  return (
    <h1
      className={`w-fit rounded-3xl bg-greenprime p-2 px-5 font-['Rubik_Mono_One'] text-xl text-white ${custom}`}
    >
      FIDEA
    </h1>
  );
};

export default WebLogo;
